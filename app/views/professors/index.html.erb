<h1>Professors</h1>
<% @professors.each do |professor| %>
  <h2>Name:<%=link_to professor.name, "/professors/#{professor.id}"%></h2>
  <%= link_to "Update #{professor.name}", "/professors/#{professor.id}/edit", method: :get %><br>
<% end %>
