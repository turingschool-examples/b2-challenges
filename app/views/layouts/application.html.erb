<!DOCTYPE html>
<html>
  <head>
    <title>BaseNew</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>
  <section>
    <p><%= link_to "HOME", "/" %></p>
    <p><%= link_to "Index", "/mechanics" %></p>
  </section>
    <%= yield %>
  </body>
</html>
