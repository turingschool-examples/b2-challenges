<h1>Mechanics</h1>

<p>Name: <%= @mechanic.name %></p>
<p>Years Experience: <%= @mechanic.years_experience %></p>
<p>Current rides they’re working on:</p>
<% @mechanic.rides.each do |ride|%>
<%if ride.open %>
  <p><%= ride.name %></p>
  <%end%>
<%end%>

<h1>Add ride to workload:</h1>
<%= form_with url: '/rides', method: :post, local: true do |form| %>
<%= form.label :name %>
<%= form.text_field :name %>
<%= form.label :thrill_rating %>
<%= form.text_field :thrill_rating %>
<%= form.label :open %>
<%= form.text_field :open %>
<%= form.submit 'Add Ride' %>
<% end %>
