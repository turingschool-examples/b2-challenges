<%= form_with url:"/ride_mechanics", local: true do |f| %>
  <%= f.label :rideid %>
  <%= f.text_field :rideid %>
  <%= f.hidden_field :mech_id, value: "#{@mech_id}" %>
  <%= f.submit 'Submit' %>
<% end %>