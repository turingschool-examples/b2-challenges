require 'rails_helper'

RSpec.describe 'The show page for mechanics,' do

  before :all do
  end

  before :each do
  end

  after :all do
  end

  it 'has a header with the name of the mechanic'

  it 'shows years of experience'

  describe 'current rides they are working on,' do
    it 'has a header'

    it 'shows rides this mechanic is working on'

    it 'displays list of rides in descending order by thrill rating'

    it 'only displays rides that are open'
  end
end
